import setuptools

setuptools.setup(
    name='EFTmva',
    version='0.0.0',
    description='Framework and tools to build trained likelihood-free inferences DNNs',
    packages=setuptools.find_packages()
)